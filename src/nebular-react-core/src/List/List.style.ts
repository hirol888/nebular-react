import { createStyles } from '@nebular-react/styles';

export default createStyles(() => ({
  root: {
    display: 'flex',
    flexDirection: 'column',
    flex: '1 1 auto',
    overflow: 'auto'
  }
}));
