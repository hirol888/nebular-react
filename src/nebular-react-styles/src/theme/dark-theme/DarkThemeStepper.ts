import { DefaultThemeStepper } from '../default-theme';
import { NebularThemeStepper } from '../types/nebular-theme';

export const DarkThemeStepper: NebularThemeStepper = {
  ...DefaultThemeStepper
};
