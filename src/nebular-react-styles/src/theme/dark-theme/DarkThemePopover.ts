import { DefaultThemePopover } from '../default-theme';
import { NebularThemePopover } from '../types/nebular-theme';

export const DarkThemePopover: NebularThemePopover = {
  ...DefaultThemePopover
};
