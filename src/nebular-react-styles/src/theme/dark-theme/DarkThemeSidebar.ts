import { DefaultThemeSidebar } from '../default-theme';
import { NebularThemeSidebar } from '../types/nebular-theme';

export const DarkThemeSidebar: NebularThemeSidebar = {
  ...DefaultThemeSidebar
};
