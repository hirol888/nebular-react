import { DefaultThemeLoader } from '../default-theme';
import { NebularThemeLoader } from '../types/nebular-theme';

export const CorporateThemeLoader: NebularThemeLoader = {
  ...DefaultThemeLoader
};
