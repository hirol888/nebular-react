import { DefaultThemeAlert } from '../default-theme';
import { NebularThemeAlert } from '../types/nebular-theme';

export const CorporateThemeAlert: NebularThemeAlert = {
  ...DefaultThemeAlert,
  alert_shadow: 'none'
};
