import type { ButtonStylesNames } from '@nebular-react/core';

export const Button: Record<ButtonStylesNames, string> = {
  root: 'Root element',
  wrapper: 'Wrapper for root element',
  fullWidth: 'Full width',
  disabled: 'Disabled status',
  wrapperDisabled: 'Disabled status for wrapper'
};
